# Prompts schemas

Range ici un fichier par schema (ex. `marketing-funnel-simple.txt`).

Chaque prompt doit :
- expliquer le schema en etapes claires
- rester lisible pour un debutant
- preferer des formes simples (fleches, boites, chiffres)
