# Chapitre 7 - Boucles

```java
for (int i = 0; i < 5; i++) {
  System.out.println(i);
}
```

`while` repete tant que c'est vrai. Chez DanielCraft, Sam dessine le compteur. Max cree une boucle infinie une fois : Ctrl+C, lecon.

:::retenir
Boucle = repeter. Surveille la condition d'arret.
:::

## A toi

Affiche les nombres de 1 a 10.

:::attention
Oublier d'avancer le compteur dans while = boucle infinie.
:::
