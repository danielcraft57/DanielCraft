# Chapitre 5 - Types de base

`int`, `long`, `double`, `boolean`, `char`, `String` (objet texte). Chez DanielCraft, on reste sur ces briques. Lea calcule un total `double`. Max compare des `boolean`. Sam rappelle : `String` n'est pas un type primitif, mais tu l'utilises partout.

:::retenir
Choisir le type selon l'usage : compteur, montant, texte, vrai/faux.
:::

## A toi

Pour un stock de produits : quels types pour quantite, prix, nom ?

:::astuce
Montants : `double` pour debuter ; plus tard tu verras BigDecimal.
:::
