# Chapitre 8 - Methodes

Une **methode** regroupe des instructions. Tu l'appelles avec un nom.

```java
static int doubleDe(int x) {
  return x * 2;
}
```

Chez DanielCraft, Lea decoupe "lire", "calculer", "afficher". Max copie-colle trois fois : Sam force une methode.

:::retenir
Methode = ecrire une fois, appeler souvent.
:::

## A toi

Ecris `max(int a, int b)` qui renvoie le plus grand.

:::astuce
Noms de methodes = verbes (`calculerTotal`).
:::
