# Quiz final

Pas de piege. Relis une fois si besoin. L'idee : verifier que tu peux avancer sans paniquer.

## Questions

1. Pourquoi preferer `header`, `main`, `nav`, `footer` a une pile de `div` anonymes ?
- A) Parce que ca change automatiquement les couleurs
- B) Parce que ca donne un sens clair a la structure (humains et machines)
- C) Parce que Flexbox refuse les `div`

2. A specificite egale, quelle regle gagne en general ?
- A) La premiere du fichier
- B) La derniere du fichier
- C) Celle ecrite en anglais

3. Ou declare-t-on souvent les variables CSS globales ?
- A) Dans `:root`
- B) Uniquement dans chaque balise `<img>`
- C) Dans le fichier `robots.txt`

4. `display: grid` sert surtout a :
- A) Remplacer HTML par CSS
- B) Poser un layout en lignes et colonnes
- C) Compresser les images

5. Dans `grid-template-areas`, chaque ligne du dessin doit :
- A) Avoir le meme nombre de cellules
- B) Commencer par un `#`
- C) Etre vide

6. Pour un menu horizontal logo + liens, le reflexe le plus naturel est souvent :
- A) Flexbox
- B) Une table HTML a 12 colonnes
- C) `position: fixed` sur chaque lien

7. Pour des vignettes produit de meme taille, un bon duo est :
- A) `object-fit: cover` dans un cadre fixe / `aspect-ratio`
- B) `object-fit: fill` toujours, meme si ca ecrase
- C) Largeur fixe 2000px sur mobile

8. Un vrai `label` de formulaire, c'est surtout :
- A) Un placeholder qui disparait
- B) Un texte relie au champ (souvent via `for` / `id`)
- C) Une image decorative

9. Ou placer en general la propriete `transition` pour un aller-retour doux ?
- A) Uniquement dans `:hover`
- B) Sur l'etat de base (ex: `.bouton`)
- C) Dans la balise `<title>`

10. `:focus-visible` aide surtout a :
- A) Masquer le focus pour toujours
- B) Montrer clairement le focus clavier sans bruler tout le design
- C) Activer le mode sombre

11. Pour un dark mode maintenable, le meilleur levier est :
- A) Dupliquer toutes les classes en `.dark`
- B) Redefinir les variables CSS (media et/ou classe)
- C) Mettre `filter: invert(1)` sur `body` et prier

12. Bonne pratique generale :
- A) `!important` partout pour aller plus vite
- B) Variables + HTML semantique + test clavier regulier
- C) Tout positionner en `absolute`

## Corriges

1-B, 2-B, 3-A, 4-B, 5-A, 6-A, 7-A, 8-B, 9-B, 10-B, 11-B, 12-B.

Si tu as 9/12 ou plus : tu es pret pour des pages reelles plus propres. Sinon, relis les chapitres lies (semantique, cascade, variables, Grid, images, formulaires, a11y, dark mode), sans dramatiser.

## Mini debrief

Entoure deux questions ratees. Rouvre le chapitre correspondant. Refais l'exercice "A toi" de ce chapitre. Le quiz n'est pas un examen : c'est une carte des trous a boucher.
