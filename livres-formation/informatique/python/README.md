# Python - Les bases (+ un cran)

Livre debutant Python, avec notions un peu plus avancees (JSON, exceptions, classes).
Theme couleurs : bleu encre + abricot (pas le vert HTML/CSS et JS).

## Rebuild

```powershell
python livres-formation\informatique\python\build_livre.py
```

PDF : `livres-formation/pdf/python-les-bases.pdf`
