# Quiz final - Teste-toi !

Pas de piege. L'idee : verifier que tu pilotes Python avec methode - syntaxe, types, structure, fichiers, exceptions, classes. Chez DanielCraft, un quiz sert a reperer les trous, pas a humilier. Lea note ses erreurs et rouvre deux chapitres. Max refait les ateliers. Sam utilise ce QCM en fin de module. Toi, tu prends un stylo, tu coches, tu respires. Ensuite seulement, tu compares.

Reponds sans regarder. Une premiere passe honnete vaut mieux qu'une triche qui te rassure cinq minutes. Si une question te bloque, marque-la et continue. Tu reviendras. Le but n'est pas 18/18 du premier coup. Le but, c'est de savoir ou reprendre.

:::astuce
Fais le quiz une fois, note ton score, puis rejoue dans une semaine. Le vrai progres se voit a la deuxieme passe.
:::

## Questions

1. `print("Salut")` sert a :
- A) Lire un fichier
- B) Afficher a l'ecran
- C) Installer Python

2. `input(...)` renvoie surtout :
- A) un int
- B) un str (texte)
- C) un bool

3. Pour convertir `"12"` en nombre :
- A) `str(12)`
- B) `int("12")`
- C) `print("12")`

4. Apres `if age >= 18:` on doit :
- A) Indenter le bloc
- B) Fermer avec `end`
- C) Mettre une virgule

5. `range(1, 4)` donne :
- A) 1, 2, 3, 4
- B) 1, 2, 3
- C) 0, 1, 2, 3

6. `return` dans une fonction :
- A) Affiche toujours
- B) Renvoie une valeur
- C) Efface la variable

7. Dans `["a", "b"]`, index de `"a"` :
- A) 1
- B) 0
- C) 2

8. Un dictionnaire associe :
- A) index numerique seulement
- B) cles -> valeurs
- C) uniquement des bool

9. `with open(...) as f:` c'est bien parce que :
- A) Ca colore le texte
- B) Ca gere l'ouverture/fermeture proprement
- C) Ca remplace `print`

10. `random.randint(1, 6)` :
- A) Tire un entier de 1 a 6
- B) Affiche toujours 6
- C) Cree un fichier

11. `True` et `False` s'ecrivent :
- A) true / false
- B) True / False
- C) TRUE / FALSE seulement

12. Indentation en Python :
- A) Optionnelle
- B) Importante (fait partie de la syntaxe)
- C) Interdite

13. `try/except` sert surtout a :
- A) Accelerer le code
- B) Rattraper une erreur sans tout planter
- C) Creer une variable

14. Dans une classe, `__init__` :
- A) Supprime l'objet
- B) Initialise l'objet a la creation
- C) Importe un module

15. `json.dump` sert a :
- A) Lire un JSON depuis un fichier
- B) Ecrire un objet Python en JSON dans un fichier
- C) Afficher du HTML

16. `liste[-1]` :
- A) Erreur toujours
- B) Dernier element
- C) Premier element

17. `score += 1` equivalent a :
- A) `score = score + 1`
- B) `score = 1`
- C) `print(score)`

18. `break` dans une boucle :
- A) Recommence le tour
- B) Sort de la boucle
- C) Quitte Python

## Reponses

1 B, 2 B, 3 B, 4 A, 5 B, 6 B, 7 B, 8 B, 9 B, 10 A, 11 B, 12 B, 13 B, 14 B, 15 B, 16 B, 17 A, 18 B

## Score

16-18 : nickel. 12-15 : bien, relis exceptions + classes + JSON. 8-11 : refais 5, 6, 8, 11, 15. Moins de 8 : reprends les chapitres 3 a 10 tranquillement. Puis rejoue le quiz dans une semaine. Chez DanielCraft, le vrai score, c'est la deuxieme passe.

## Petite histoire

Lea a fait 11/18 la premiere fois. Elle a rit. Puis elle a relance le juste prix, relu les exceptions, rejoue : 16/18. Max a rate la question sur `return` parce qu'il confondait encore avec `print`. Il a ecrit trois fonctions qui renvoient vraiment. Sam affiche le score moyen de la classe sans nommer personne : "on revoit indentation et dicos demain". Trois reactions, meme message : le quiz est une carte, pas un jugement.

## En vrai

Coche tes mauvaises reponses. Ouvre le chapitre lie. Refais un mini exercice de cinq minutes. Pas besoin de tout relire. Un trou a la fois. Si tu as rate `try/except`, plante `int("bonjour")` volontairement. Si tu as rate les dicos, recree un mini carnet contacts. Le geste ancre mieux qu'une relecture passive.

## A toi

Ecris tes trois questions ratees (ou les plus floues). A cote, une phrase "ce que je confonds". Garde ce papier. Dans sept jours, rejoue seulement ces trois. Chez DanielCraft, on prefere trois trous bouches a un 18/18 oublie le lendemain.
